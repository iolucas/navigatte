//Main navigatte modules that defines all general things needed

var Navigatte = {}

/*Navigatte = new function() {

	var initActions = [];
	var self = this;

	//Public variables to store nodes and links 
	this.nodes;
	this.links;

	this.OnInit = function(callback) {
		if($.isFunction(callback))
			initActions.push(callback);
	}

	this.Init = function() {
		self.nodes = [];
		self.links = [];

		for(var i = 0; i < initActions.lengnth; i++)
			initActions[i].call(self);
	}
}*/